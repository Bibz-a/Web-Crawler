# Web-Crawler
A C++ web crawler that fetches webpages, extracts hyperlinks, builds a graph of site connectivity, and organizes results using core data structures like queues, stacks, hash maps, and sorting algorithms.
